# Agent Notes

This is a standalone Cloud & Core Studio app. Keep changes small, keep the app buildable, and do not commit local environment files.

Use `bun install`, `bun run lint`, and `bun run build` before pushing meaningful changes.
